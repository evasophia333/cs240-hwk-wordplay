//Given a word find all possible other words 
//Create an array with a list of the letters that are in the word 
//Go through each word bigger than 3 characters. See if the word contains any of the letters. If so then add that word to the array of possible other words 
// 
function randomWord() {
    var randomNum = Math.random();
    var item =
        console.log(item);
};


